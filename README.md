# `@selfhosted/dev-prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ pnpm add --save-dev @selfhosted/dev-prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@selfhosted/dev-prettier-config"
}
```
